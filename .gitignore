# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# akiadvise skill scratch report (single overwritten file, not a doc source of truth)
ADVISE.html

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Disposable visual export from /akihtmlreport - regenerated wholesale each run, not a doc source of truth (docs/ is)
REPORT.html

# This repo is itself a project the app can manage, so it grows its own runtime notes file.
# It holds the owner's real task list for this repo, not a fixture - see docs/feat/project-task-list.md
# "Self-hosting wrinkle" for why that is left uncommitted here despite the feature's own design intent
# ("meant to be committed") for every OTHER project this app manages.
.akidevsync/
