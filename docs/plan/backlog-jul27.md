# Backlog — jul27 batch (terminal ownership · usage OOP · IME · hygiene)

Master index for a seven-item request delivered through `/akiflow tier=2`. This doc is the **tracker**: every workstream below owns one plan doc, and status changes land here first so no stage drifts out of sight. Individual plan docs hold the design; this file holds only scope, owner, status and the dependency order.

**Version state**: `package.json` is `1.21.0`; everything here accumulates in `CHANGELOG.md`'s `[Unreleased]` buffer. No version is minted until the release event (`release.A`) — do not name new files after a version number that does not exist yet.

**Baseline warning**: the working tree was already half-finished when this batch opened (notes-JSON SSoT work, IME guard v2, terminal cap work, external-terminal modal — all uncommitted). Nothing here may `git add`/`stash`/`checkout`/`reset`. Any workstream that needs a clean baseline says so in its own doc.

## Sizing gate

`[akiflow] tier=2 (forced by user)` — signals present: new user-facing flow (terminal chrome settings menu), architecture rewrite across ≥3 modules (external-terminal ownership; `agent_usage.rs` split), and an unresolved native-layer bug. **Market counsel is deliberately NOT invited** (`akiflow` step 2, law 3): this is an internal developer tool with no pricing, positioning or audience decision in scope. UX-Psych *is* invited — WS-A and WS-B both turn on defaults and on what a badge is allowed to claim.

## Workstreams

| ID | Scope | Plan doc | Status |
|---|---|---|---|
| WS-A | External-terminal ownership model: spawn-origin registry in RAM, per-project vs global counting, one shared terminal-button class | `terminal-ownership-model.md` | **planned** |
| WS-B | Terminal chrome visibility: 3-dot settings menu on the stack header, per-control checkboxes, host-dependent defaults (browser = all shown, app = all hidden) | `terminal-chrome-settings.md` | **planned** |
| WS-C | Usage probe OOP: `get-antigravity-usage.js` → shell, delete `Interpreter::Node`, split `agent_usage.rs` (892 lines) into domain modules behind a shared `UsageProbe` trait | `done/usage-probe-oop.md` | **shipped 2026-07-29 in `610fd93`** — seven modules plus `remote_shell.rs`; the trait was argued and rejected, and that rejection is upheld. Read `done/`, not the untracked copy at `plan/usage-probe-oop.md`. Two defects survive the port and are open: the `agy` branch never extracts the CSRF token (P10, see D-12), and `claudecode.rs:45-52` still carries `agent_name` and an `"antigravity"` string |
| WS-F | A corner indicator on PUSH and PULL showing whether that direction runs with `--delete` | §"WS-F" below | **implemented** |
| WS-D | Vietnamese IME residual bug: restore-after-backspace loses a character in the in-app terminal only | `docs/research/terminal-vietnamese-ime-root-cause-4.md` (head of chain; `-3.md` superseded) | **shipped and closed 2026-07-31** — superseded as chain head by `terminal-vietnamese-ime-root-cause-5.md`; text-drain architecture (`4851f40`), the double-space blocker (`b99502a`), and the input-mode A/B escape hatch removal (`0a6d314`) all landed. `docs/plan/terminal-input-jul31.md` §1 confirms the full `#3 → #13 → #2 + #8` order "đã hoàn tất trên thực tế" (completed in practice), owner-verified on real hardware. Two residual, separately-tracked items survive: §2.2 Android/Gboard (open, not yet traced) and §2.3 Ctrl/Shift latch display (fixed, pending confirm) — neither blocks calling WS-D itself done |
| WS-E | Hygiene batch: companion WebSocket reconnect noise, project-icon 404 logged as an error despite a working Rust fallback, hard-wrap sweep across recently touched files | `hygiene-jul27.md` | **all 3 resolved — see the note below.** Closed |

### WS-E status correction

Item 1 (WebSocket reconnect noise) closed with no code change — `hygiene-jul27.md` records the re-trace. Item 2 (project-icon 404) has its fix target modified in the working tree (`src/utils/projectIcon.js`). Item 3 (hard-wrap sweep) **is settled: finished, not abandoned.**

**Evidence.** Two commits landed the sweep: `1c00e3b` ("docs: hard-wrap prose sweep across plan/arch/feat/ref/research, sync 1.21.1's move to done", 2026-07-28 15:38) and `3c5188e` ("docs: hard-wrap prose sweep for CLAUDE.md", 2026-07-28 15:47) — both inside the `1.20.0..HEAD` range, both present in `git log` today. `1c00e3b` alone reflows 15+ files (`docs/arch/usage-antigravity.md`, `docs/feat/remote-control.md`, `docs/plan/done/1.20.0-terminal-and-remote-sync.md`, …). Re-checking `docs/ref/dragging-titlebar.md` today (the file the original uncommitted-diff pass spot-checked) still shows one-line-per-paragraph prose with no mid-sentence break, confirming the rejoin held after commit and was not later reverted. The uncommitted 49-file diff described when this note was first written is gone from the working tree because it is now history, not because it was discarded.

Two consequences from the original note, now stale in light of the above, corrected here rather than reworded in place so the reasoning trail stays legible:

1. **The sequencing rule ("hard-wrap sweep must run last") did get inverted in practice** — it ran first, on 2026-07-28, before the terminal/usage/UI batch's own doc edits. That part of the original note still holds: any edit to those docs must be written against post-sweep content.
2. **Item 3's classification is now made, on evidence, not guessed.** The commit hashes and dates above are the fact this was previously missing; `agent.B5`'s unclassified default applied only while the tree was ambiguous, and it no longer is.

## Dependency order

- ~~WS-D is **blocked on runtime evidence from the Mac** (`__akiIme.dump()`).~~ **No longer true, 2026-07-30.** The evidence was never needed: the question was closed from engine source. WS-D is now blocked only on the ordering in R-9 (#3 ships first) and on gating the `aki-input-mode` A/B so the change has a runtime revert.
- WS-B depends on WS-A's control inventory: the settings menu can only toggle controls that exist, and WS-A may add or remove some.
- WS-C and WS-E are independent of everything else and can land in any order.
- The hard-wrap sweep in WS-E must run **last** among the code workstreams, or it will collide line-for-line with every other diff.

## Item → workstream trace

Every item in the original request maps to exactly one workstream, so nothing silently drops:

1. Companion WebSocket `ws://127.0.0.1:1421/ws?role=host` — "network connection was lost" → **WS-E**
2. Project-icon 404 logged as an error although the Rust side already falls back → **WS-E**
3. Auto-wrapped lines violating `agent.C`'s no-auto-wrap rule → **WS-E**
4. Usage refactor: AG probe to shell, `Interpreter::Node` removal, `agent_usage.rs` decomposition → **WS-C**
5. Vietnamese typing still broken (`e x x i t` → loses the restored `x`), VS Code's terminal as the reference implementation → **WS-D**
6. Terminal chrome show/hide as an option with a settings menu → **WS-B**
7. External terminal tracking by spawn origin rather than by `cwd` scan → **WS-A**

## WS-F — the `--delete` corner indicator on PUSH and PULL

Added mid-batch, at the user's request: *"tôi muốn flag DELETE có hiện 1 icon nhỏ ở góc của nút pull|push"*. Small enough that it gets a section here rather than a plan doc of its own (`docs.C1` — two docs for one question is the failure that bound exists to prevent).

**Why it is worth having, beyond being asked for.** `delete_on_pull` defaults to **`true`** and `delete_on_push` to **`false`** (`src/composables/useProjectConfig.js`, and `src-tauri/src/projects.rs`'s field comment). So the more destructive of the two directions is the one that is armed by default, and today nothing on the button says so — the setting is two clicks away behind the gear. `src/components/ProjectTable.vue:373` already records the reasoning that a mistaken `--delete` mirror is the outcome most feared here. This puts the arming state where the decision is actually made.

**Shape — no new element in the flow, per CLAUDE.md's Extreme Narrow rule.** Both buttons are already wrapped in `CountBadgeWrap.vue`, which supplies the `position: relative` anchor and renders the red pending-count badge at `.cell-badge-top` (top-right, `main.css:833`). The indicator therefore goes in the **bottom-right** corner, where `main.css:837`'s `.cell-badge-bottom` already exists and is unused by these two buttons — so it costs one absolutely-positioned overlay, one existing class, and zero layout.

**Spec:**

- Rendered only when that direction's flag is on: the PUSH button's indicator follows `delete_on_push`, the PULL button's follows `delete_on_pull`. Off means no element at all, not a dimmed one — an unarmed direction should look exactly as it does today.
- Icon `fa-solid fa-trash` or `fa-scissors` at the badge's 9px scale, in the danger colour the app already uses for destructive state (`#ef4444`, the same value `CountBadgeWrap`'s count badge uses locally — reuse the token, do not introduce a second red).
- `title` states the consequence, not the flag name, per `RULE-content-write.md` B1: on PULL, "Mirror: files here that the remote does not have will be deleted." On PUSH, the same sentence with the sides swapped.
- It must not collide with the STOP state (`ProjectTable.vue:1222` — the same button turns red while syncing). Decide one of: hide the indicator while that direction is running, or keep it. Recommendation: **hide it** — during a run the button's whole meaning is "stop", and a destructive-mode marker on a STOP button reads as though stopping is what deletes something.
- Also check the narrow breakpoint (`ProjectTable.vue:1315`), where the DRY toggle is already squeezed so the top-right count badges have room. A second badge on the opposite corner needs the same check, and this is the one part that can only be judged on screen.

**Acceptance criteria:**

- With `delete_on_pull: true` (the default) a fresh project shows the indicator on PULL and not on PUSH.
- Toggling either flag in the gear updates the corresponding button's indicator with no reload.
- No new DOM element exists when both flags are off; no button changes width in any state (Extreme Narrow).
- No new colour value and no new `z-index` enter the diff.
- `README.md` and `IntroModal.vue` checked in the same task (CLAUDE.md, "Feature changed?"), plus a `CHANGELOG.md` `[Unreleased]` entry.

**Sequencing**: independent of every other workstream, but it touches `ProjectTable.vue`, which WS-A's step S5 also touches. Land it either before WS-A/S5 or after — not concurrently.

## Decisions taken by the user, 2026-07-27

Recorded here rather than only in the workstream docs, because each one overrides something a plan doc argued and a later reader needs to know the argument was heard and settled.

- **The compose input stays shown by default in the Mac app**, against the user's original "app hides everything" rule and following `terminal-chrome-settings.md` §6.2. The other six controls hide as originally stated. Consequence for WS-B: the app-side default map has exactly one `true` entry, and §6.3's one-time armed tint is no longer needed for the compose case — keep it only if the menu still tests as undiscoverable.
- **A terminal opened over SSH from a project's popup counts on that project's badge**, resolving `terminal-ownership-model.md` §11 question 1 in favour of the spawn-origin rule. This is a deliberate change in what the number claims — from "windows standing in this folder" to "windows I started from here" — and the CHANGELOG entry must say so plainly, since today such a window is counted nowhere at all.
- **Execution order: WS-D, WS-E and WS-F first**, then the larger workstreams, with WS-E's hard-wrap sweep still last of everything. **Amended 2026-07-30**: WS-D is no longer atomic — it is the `#13` slot of the terminal-input order in R-9 (`#3 → #13 → #2 + #8`), so the sticky-modifier fix precedes it. Nothing else in this line changes.

## The terminal / usage / UI batch — thirteen issues

A second request, planned by a tier-2 council. It is tracked here rather than in a new tracker of its own: this file is already the project's live plan index, and a second index covering an overlapping set of docs is the two-trackers-for-one-question failure `docs.C1` exists to prevent. The jul27 workstream table above is untouched — these are separate rows with their own IDs so provenance stays legible.

Three of the thirteen produce no plan doc at all, and each says why in its row. A deferral that is merely absent from a table is indistinguishable from an oversight (`docs.C2`).

| ID | Issue | Plan doc | Status |
|---|---|---|---|
| T-1 | Terminal compose input: remove the custom typeface declarations (#2) and accept Shift+Enter (#8) | `terminal-input-surface.md` | **closed, owner-verified 2026-07-31** — `docs/plan/terminal-input-jul31.md` §1: compose-row font "nhất quán, hoàn hảo", Shift+Enter "hoàn hảo" |
| T-2 | Sticky/latching modifier keys (#3) | `terminal-input-surface.md`, contingent section | **closed, owner-verified 2026-07-31** — the phone-vs-Mac question resolved moot: `terminal-input-jul31.md` §1 confirms sticky Ctrl/Shift correct at the logic layer with cross-program evidence (Shift+Tab in Claude Code, Ctrl+X in `nano`, Ctrl+C from the compose box) |
| T-3 | AG quota frozen while `agy` runs in the in-app terminal (#4) | `done/ag-usage-pin-vs-live.md` | **closed** — `de801c2` ("docs(ag-usage): close pin-vs-live plan, add CHANGELOG entry"), plan doc moved to `done/` |
| T-4 | Claude Code account/email never re-derived on refresh (#9) | `cc-account-identity-ssot.md` | **fix implemented, awaiting the owner's on-device test.** Root cause was narrower than assumed: identity came from `auth-cache.json` behind a 300s TTL plus a once-per-app-launch latch, while quota came from `rate-limits-cache.json`, which the statusLine hook rewrites independently — two sources, two cadences. The Rust parser and the JS store were both already correct and were not changed |
| T-5 | DEV/BUILD disabled-not-hidden in the OPEN popup (#6) | `done/dev-build-visibility.md` | **shipped, verified on macOS by the owner 2026-07-30** |
| T-6 | DEV/BUILD execute in the in-app PTY (#7) | `done/dev-build-in-app-launch.md` | **shipped, verified on macOS by the owner 2026-07-30.** Ran before WS-A as required; §2a of `terminal-ownership-model.md` now records the amendment as applied, and `run_project_command`/`run_project_dev`/`run_in_project_terminal` are removed from `system.rs` |
| T-7 | Four sites where an existing UI contract was not honoured: project-column flex (#5), `user-select` on the confirm-delete phrase (#10), GlobalNote's missing narrow repeat and its textarea min-height (#11) | `ui-sweep-misses.md` | planned |
| T-8 | Hide/reveal a project row (#12) | `project-visibility-toggle.md` | **closed, not built.** The owner resolved the gate by removing the hide-a-project feature outright (`49025a2`), so the Extreme Narrow question this row was blocked on no longer has a subject |
| T-9 | Terminal resize must reach both the app and the companion (#1) | **none — verification, not a plan.** See below | static analysis found no defect at any candidate origin; one runtime check outstanding |
| T-10 | Vietnamese typing in xterm (#13) | **none — already WS-D above** | **closed, owner-verified 2026-07-31** — see the WS-D row above; `terminal-vietnamese-ime-root-cause-5.md` is the chain head |

### Execution order

**Unblocked, in order:** T-4 (#9) → #10 → T-5 (#6) → T-7's remaining three → T-1 (#2 + #8).
**On the owner's answers:** T-3 (#4) → T-6 (#7, after T-5) → T-2 (#3) → T-10 (#13) → T-8 (#12).

**Remaining as of 2026-07-31:** T-1, T-2, T-3, T-5, T-6, T-9 and T-10 are all shipped/closed. T-8 is closed without being built. Only **T-4** and **T-7** are still open — T-4 awaits the owner's rebuild+Apply+on-device verify against `cc-account-identity-ssot.md` §14's corrected path (plan doc still active, not yet moved to `done/`); T-7's status was not re-checked in this pass (`ui-sweep-misses.md` untouched from `docs/plan/`, still active).

Severity is deliberately overruled in two places, named so a later reader does not read them as mistakes:

- **T-6 runs before T-2** despite being the less severe of the two, because it gates an entire already-planned workstream (WS-A). Sequencing a whole workstream's correctness outranks the ordering of two items inside one file.
- **#10 runs before T-5** on cost, not importance — it is one class on one element and unblocks a *destructive* confirm dialog. Cost may outrank blocked work; it must never outrank unblocked work of higher severity, which is why T-4 sits above it.

Four items are blocked on owner answers rather than on engineering, and three of the four are thirty-second questions. Batching them into one message unblocks most of the schedule at once: (1) T-2's surface; (2) T-10 — run `scripts/capture-ime-evidence.sh` and paste `__akiIme.tail(20)`; (3) T-3 — run `DEBUG=1 node scripts/get-antigravity-usage.js` with `agy` live; (4) T-8 — must a hidden project be discoverable?

### T-9 — the verification task for terminal resize (#1)

**The honest close is "no defect found statically, one runtime check outstanding" — not "no bug".** The distinction matters: the second phrasing would let a real runtime symptom be dismissed by pointing at this doc.

What static analysis established, with sources, across two rounds: `FRAME_PTY_RESIZE` exists and is wired in both directions; the host is the sole resize authority and a companion is *structurally* barred from calling `pty_resize` because it is absent from `COMPANION_ALLOWED_COMMANDS`; resize frames are on the never-dropped list, so congestion cannot silently eat one; and geometry is pushed unprompted on every new socket connection — including every reconnect — through `pushAllScrollbacks`, so a late joiner converges without asking. All four hypotheses about where propagation might break were refuted against named files, not waved off.

That leaves a runtime check the dev box cannot perform. Four parts, on a real Mac with a real phone paired — `scripts/verify-pty-resize.sh` prints the numbered walkthrough for exactly this checklist, to run by hand on the Mac:

1. **Host-side origins that change the DOM box.** Drag the dock splitter; collapse the whole stack and expand it; switch tabs; apply a window-size preset. After each, confirm the companion's rendered grid matches the Mac's. These are the four origins static reading cleared; this is the check that they are cleared in practice.
2. **Late join and reconnect.** Resize on the Mac *first*, then pair a phone — its geometry should be correct on arrival, unprompted. Then background the phone until the socket drops and let it reconnect; geometry should arrive again without a resize event.
3. **Font zoom on the companion, using its own key-row `+`/`−`.** This is the one genuinely unsettled item: whether xterm re-measures and repaints on a bare `fontSize` option write with no accompanying `fit()`/`resize()`. Watch whether the rendered glyph size actually changes. If it does not, that is a real leaf defect and it is a *different* symptom from the one the request describes.
4. **Which build was observed.** Confirm whether #1 was seen before or after the font-zoom commit `8cc2669` and the uncommitted dock-splitter work. If before, the reported symptom may already be gone, and parts 1–3 will show it.

If all four pass, T-9 closes as verified-with-no-change — and per the batch's CHANGELOG rule that outcome gets **no** entry, because a verification is not a fix.

### A named optional optimisation — not a defect, not a required fix

`hostResize()` performs no cols/rows equality check: every `doFit()` cycle with `ownsPtySize` true unconditionally invokes `pty_resize` and broadcasts a frame, whether or not the geometry actually changed. During a continuous gesture such as a dock-splitter drag that is a real `TIOCSWINSZ` plus a socket send per animation tick.

**The current behaviour is correct — merely chatty.** Nothing is missed; the risk here is the opposite of a silent skip. It is recorded as an optional optimisation with two deliberate qualifications:

- It was first dismissed by citing `flow.C2`'s red flags against added enforcement. **That citation was wrong**, and the correction is the reason this is written down: an equality check *removes* redundant work rather than adding a guard against an untrustworthy happy path. `flow.C2` does not apply, so "it would be a C2 violation" is not available as a reason to skip it.
- Two reviewers reached this same churn point independently, from opposite directions. That convergence is why it is named rather than dropped — but convergence on "this is chatty" is still not evidence that it costs anything measurable, and nobody has measured it. Anyone picking it up should measure first.

### Constraint carried by T-6, recorded so this row cannot contradict its own plan doc

T-6's design writes the DEV/BUILD command into the PTY the moment `pty_spawn` resolves — the earliest possible moment in that shell's life. `npm run dev` under nvm is the single most PATH-sensitive command this app issues, and CLAUDE.md's cold-start subprocess PATH race is ABSOLUTE: the symptom is an intermittent `command not found` that self-heals within minutes and is not reproducible when the same command is tried manually later. The shared-preamble resolution is therefore a **binding constraint on T-6's first draft**, not a follow-up fix, and `dev-build-in-app-launch.md` carries it. This row exists only so a reader of the tracker does not conclude otherwise.

## Findings — read-only, recorded not fixed (`agent.B5`)

Surfaced while mapping the thirteen issues onto this tracker, then extended by the 2026-07-30 consolidation pass. Severity uses `docs.C4`: **wrong 1 · stale 5 · incomplete 4 · cosmetic 1** (was wrong 0 · stale 3 · incomplete 3 · cosmetic 0 before the consolidation pass). D-1, D-3, D-8 and D-9 have since been closed **by action** and say so in their rows; every other row is still report-only. The count is deliberately not compressed into one number: **one finding is now `wrong`** — D-7, a duplicate plan doc declaring shipped work that is not shipped — and that is the only entry a reader could act on and be harmed by.

| ID | Finding | Severity |
|---|---|---|
| D-1 | WS-E was marked `planned` while two of its three items had resolved and the third had landed uncommitted across 49 doc files. Corrected in the table above; the sequencing consequence is in the WS-E note | stale |
| D-2 | `investigate-ag-account-switch-detection.md` has read "Open — cần Mac để test" since early July and has never been executed. `docs.C3` requires an unexecuted active plan to be labelled dead or unstarted rather than left ambiguous. It is also **not** the same root cause as T-3 — that plan is about a live `language_server` returning stale context after an in-IDE switch; T-3 is about a fresh reading being discarded downstream. Closing one does not close the other | stale |
| D-3 | `remote-views-roadmap.md` is half shipped: its Terminal View section self-marks as built, Explorer View is still a sketch. Not a `done/` move, but the shipped half now functions as a decision record other work cites — it is where the host-sole-resize-authority rule is actually written, which makes it load-bearing for T-9. **CLOSED by action 2026-07-30** — moved to `docs/arch/remote-views-roadmap.md` as target-state design; every inbound doc reference updated | incomplete |
| D-7 | ~~`docs/plan/done/usage-probe-oop.md` is a stale duplicate claiming `implemented` while the live copy reads `planning` and the port is unstarted.~~ **INVERTED — corrected 2026-07-30.** The `done/` copy is the correct one and is the only one at HEAD; the port shipped in `610fd93`. `docs/plan/usage-probe-oop.md` is the **pre-move copy that the lifecycle commit never deleted from disk**, and this table's original entry was written from it. The "verified against source" that supported it read the same class of ghost — `src-tauri/src/agent_usage.rs` (Jul 27, not at HEAD). The harm the entry warned about was real but pointed the wrong way: a reader reaching the *active* copy first concludes WS-C is unstarted | **wrong (the finding itself)** |
| D-12 | **The working tree does not compile on this box.** `src-tauri/src/lib.rs:1` declares `mod agent_usage;` while both `src-tauri/src/agent_usage.rs` and `src-tauri/src/agent_usage/mod.rs` exist on disk — rustc E0761, ambiguous module. Static, no cargo run needed. Cause: `610fd93` removed `agent_usage.rs` and `scripts/get-antigravity-usage.js` from git but **never deleted them from disk** — both still carry pre-refactor mtimes and the `.rs` is byte-identical to the pre-refactor blob. Same shape for `docs/plan/usage-probe-oop.md`, whose move to `done/` was committed while the original stayed. A fresh clone is fine; any checkout where the delete never happened is broken, **including possibly the Mac — one `ls` there settles it**. Related but separate: `.git/index` is stale at `8cc2669` (six commits back), so every `git status` code from this box is currently meaningless. **Ordering constraint before cleanup:** the stray `.js` is the reference implementation for the JS↔sh parity check and the fixtures came from the same reading of it, so the parity run happens first — D-13 is proof that is not hypothetical. Full account: `docs/research/ghost-files-and-audit-method-jul30.md` | **wrong** |
| D-13 | **P10 is live in the shipped build, not merely a porting risk.** In `scripts/get-antigravity-usage.sh` the language-server branch extracts the token at `:93`, but the `agy` branch (`:224-250`) sets `proc_type="cli"` and never calls `extract_arg` — so `hdr_csrf` is empty at `:147-149`: no CSRF header, no seeded port. This matches issue #4 exactly ("AG cannot measure quota while `agy` runs"), in the build the owner is running now. The council's amendment to `usage-probe-oop.md` §3 warned this defect would be ported; it already was | **wrong** |
| D-8 | Two active plans whose work had already shipped in committed code sat in `docs/plan/` **and were absent from `docs/index.md` entirely** — two independent misses on the same files (`docs.B1` + `docs.A1`): `terminal-ime-input-layer-separation.md` (shipped `7ce5804`) and `terminal-mobile-keyboard-viewport.md` (shipped `873e849`). **CLOSED by action 2026-07-30** — both moved to `done/` and both indexed | stale |
| D-9 | `terminal-mobile-keyboard-viewport.md`'s own title and opening paragraph read "not yet implemented" / "nothing here has been implemented" after all four of its §3 sub-items had shipped. A reader would have re-implemented finished work. **CLOSED by action 2026-07-30** — corrected in a header block, body untouched | stale |
| D-10 | Six references now point at pre-move plan paths and could not be corrected in a docs-only pass: `src/components/TerminalView.vue:332`/`:468`, `src/composables/useTerminalInput.js:4`, `src/composables/useVisualViewportHeight.js:8` and `src/assets/main.css:126` are source comments (out of scope for this pass — fix in the next code-touching task, one `sed`); `docs/research/terminal-vietnamese-ime-root-cause-2.md:1` is a research doc and must stay as written (`docs.B2` immutability). `CHANGELOG.md:369` names the audit's old path inside a released section and is deliberately left alone | cosmetic |
| D-11 | Two open findings from the release-window audit have no scheduled home and were at risk of vanishing with the doc's relocation: **hardcoded-hex sprawl** (416 literal colour values across `.vue` files vs 14 CSS custom properties) and **CHANGELOG entries drifting to essay length** (`release.B4/B5`). Both are recorded as `docs.B2` "No action, deliberately unscheduled" — #3 was already deferred once on blast radius in `done/1.21.1-terminal-limits-and-structure.md` §6.1, and neither is dangerous. Listed here so the deferral stays visible rather than silent | incomplete |
| D-4 | **`docs/index.md` indexes the Vietnamese-IME research chain's first and second rounds but not the third — and the third is the chain head, holding the gated patch.** A reader following the index lands on superseded reasoning whose fix is explicitly recorded as having failed on the Mac, and would redo work that is already done. `docs.A1` plus `docs.B2` | stale |
| D-5 | Three plans in `done/` each end with a live runtime test still outstanding, and the CHANGELOG carries a matching not-yet-verified note. Reported as unclassified rather than as a failure: only the owner knows whether that is accepted risk or debt. It is also the bucket T-9 and T-10 fall into | incomplete |
| D-6 | No `docs/feat/` doc owns the projects table, the app's primary surface. #5, #10 and #12 therefore have no doc-sync target under `docs.B3` | incomplete |

**Unclassified working-tree state.** Four untracked source files belong to no committed plan: `src/components/modals/ExternalTerminalsModal.vue`, `src/composables/useDockLayout.js`, `src/composables/useWkImeGuard.js`, and `scripts/capture-ime-evidence.sh`. A half-finished change and an abandoned experiment are indistinguishable from the tree alone, so these are reported as unclassified and **not** guessed at (`agent.B5`). Only the author can say which each one is.

Also checked and cleared, recorded so a later audit does not re-open them: the release-window audit's first finding is genuinely fixed (the orphaned component is gone and the stale in-app copy no longer mentions the deleted flow). **Correction, 2026-07-30**: the same-filename-under-`done/` case was read here as "an in-flight lifecycle move, not drift". That was wrong on inspection — `docs/plan/audit-1.11-1.15.md` and `docs/plan/done/audit-1.11-1.15.md` were two byte-identical copies coexisting on disk (`cmp` clean), and the same pattern held for `usage-probe-oop.md` except that there the two copies **differed in their status line**, which is D-7 above. A duplicate is not a move.

## Plan-consolidation pass — 2026-07-30

Ran to answer one question: how many of these plans are genuinely live and executable? Every disposition below was decided against source and `git log`, never against a doc's own status line — this repo has now produced status-line errors in both directions.

**Active plan count: 17 → 13.** The list of seventeen was itself a finding: two of them (`terminal-ime-input-layer-separation.md`, `terminal-mobile-keyboard-viewport.md`) were in neither this tracker nor `docs/index.md`, so nobody counting active plans from either source would have seen them.

| File | Disposition | Evidence |
|---|---|---|
| `terminal-ime-input-layer-separation.md` | → `done/` | shipped in `7ce5804`; `src/composables/useTerminalInput.js` and the `TerminalView.vue` rewiring are in `HEAD` |
| `terminal-mobile-keyboard-viewport.md` | → `done/` | shipped in `873e849`; all four §3 sub-items verified present — `useVisualViewportHeight.js`, `main.css`'s `--vvh`, `useDockLayout.js:28`/`:82`, `index.html`'s `interactive-widget` |
| `audit-1.11-1.15.md` | → `docs/research/audit-release-window-jul28.md` | not a plan: an audit's finding record is a `research/` doc (`docs.C2`), and that rule also forbids the version-first filename. Duplicate `done/` copy removed after a byte-identical `cmp` |
| `remote-views-roadmap.md` | → `docs/arch/` | not a plan: target-state design with no steps, no owner and no acceptance criteria, half of it already shipped (`docs.A2` — `arch/` holds target state) |
| the other 13 | stay active | each labelled honestly; see the two relabels below |

**No merge was performed, and that is a decision rather than an omission.** Three merges were evaluated on the merits and all three were declined for the same reason: shared symptom is not shared definition of correct. `dev-build-visibility.md` + `dev-build-in-app-launch.md` — one is a frontend visibility rule that is statically verifiable, the other an execution-path change verifiable only at runtime; they touch the same two buttons, which is a *sequencing* relationship, not a shared standard. `ag-usage-pin-vs-live.md` + `cc-account-identity-ssot.md` — already evaluated and withdrawn once in this session for exactly this reason; re-merging would repeat a caught error. `ui-sweep-misses.md` + `project-visibility-toggle.md` — four binary CSS fixes against one new capability with persistence and an affordance decision. The whole reduction therefore comes from closing shipped work and rehoming things that were never plans, which is the only kind of reduction that drops the count without dropping content.

**Two plans relabelled rather than moved**, because `docs.C3` forbids leaving an unexecuted plan ambiguous and `agent.B5` forbids resolving the ambiguity by guessing:

- `hygiene-jul27.md` — items 1 and 2 are closed (one with no code change, one with its fix sitting in the working tree). Item 3, the hard-wrap sweep, has run uncommitted across 49 doc files and is the single reason this doc is still active.
- `investigate-ag-account-switch-detection.md` — relabelled from "Open — cần Mac để test" to **unstarted, blocked on one PID check**. Whether it is *dead* is the owner's call. Its distinctness from T-3 is restated in the doc itself so the two cannot be closed as one, and two of its cited paths have moved under the in-flight probe rework.

### What the owner has to answer — one word each

Nothing below is answerable by reading the tree, which is why none of it was decided here.

1. `hygiene-jul27.md` item 3 — the uncommitted hard-wrap sweep across 49 doc files: **finished, or abandoned?**
2. `investigate-ag-account-switch-detection.md` — three weeks unexecuted: **keep, or kill?** (`usage-flow` was asked whether its reopened analysis supersedes this and was not reachable, so it comes to you unfiltered.)
3. The four untracked source files that belong to no committed plan — **finished, or scrap?** for each:
   - `src/components/modals/ExternalTerminalsModal.vue` — a modal listing external `Terminal.app` windows; the closest planned work is WS-A's ownership model, which is still `planning`, so this looks like it was built ahead of its own plan.
   - `src/composables/useDockLayout.js` — the dock's height/maximize geometry; already consumed by the shipped `--vvh` work, so this one is more likely finished than the others.
   - `src/composables/useWkImeGuard.js` — the old IME interception layer that `terminal-ime-input-layer-separation.md` replaced and explicitly kept as a `legacy` escape hatch; the question is whether the escape hatch is still wanted.
   - `scripts/capture-ime-evidence.sh` — the WS-D Mac capture kit, which is finished by design and simply has never been run.
4. ~~`docs/plan/done/usage-probe-oop.md` (D-7) — a second copy of a live plan claiming `implemented`.~~ **Settled, in the opposite direction (see D-7, D-12).** The `done/` copy is at HEAD and correct; the copy at `docs/plan/usage-probe-oop.md` is untracked and pre-refactor. It belongs with the other two ghosts below, not in this list.
5. `src-tauri/src/agent_usage.rs` and `scripts/get-antigravity-usage.js` — removed from git by `610fd93` but never deleted from disk, which is what makes the tree fail to compile (D-12). Deleting them is safe (both are byte-identical to blobs still reachable in history) but is the owner's call, and the JS↔sh parity run happens first.

## Status log

- **2026-07-27** — backlog opened, tier declared, five workstreams dispatched to their planning stages.
- **2026-07-27** — WS-F added mid-batch at the user's request and specced in the section above; scope is one overlay per sync button, no new element in the flow.
- **2026-07-27** — WS-B planned (`terminal-chrome-settings.md`). Two findings outrank the menu itself. **The tab strip must be locked on for a companion**: `⌘T`/`⌘W`/`⌘⇧[`/`⌘⇧]` fully replace it on the Mac, but a phone has none of them, so a hidden strip leaves that screen unable to open, close or switch a tab at all. And **the text-size buttons live physically inside the key row's markup**, so one checkbox covering both would take a phone's only zoom control away with it — they need separating before either becomes toggleable. The doc also argues against one of the user's stated defaults (the compose input) and against moving either existing header button into the menu; both arguments are in its §6 and §7 for the user to settle.
- **2026-07-27** — WS-C planned (`usage-probe-oop.md`). **The `UsageProbe` trait was argued and rejected**, with the reasoning recorded in that doc's §5 rather than left implicit: there are two implementors but only one polymorphic call site, everything the two probes genuinely share is already extracted as shared types and functions, and what they do not share is structural (Claude Code reads a hook-written file where mtime is meaningful and there is one account by design; Antigravity queries a live RPC with N accounts and no mtime). The module split proceeds regardless and is what would make a later trait mechanical if a third agent ever arrives. Second decision worth surfacing: **the ported shell script parses no JSON at all** — neither `jq` nor `python3` can be assumed present on either target, and this repo already has an incident for each — so the script emits raw RPC bodies inside a delimiter frame and `serde_json` shapes them in Rust, which is the pattern the Claude Code probe already uses. One item blocks the port's central assumption and must be checked before any code is written: whether `curl` exists on the Ubuntu remote.
- **2026-07-27** — WS-A planned (`terminal-ownership-model.md`). Its own §3 reconciliation rule is the answer to the registry-versus-scan trap the backlog flagged at dispatch: the scan stays the sole authority on which sessions exist, the registry only ever says who launched one, so a registry entry can change attribution but can never change a total — which is what keeps the derived-count design from regressing into the counter that only grew. Two findings feed back into other workstreams: (1) WS-B's stated dependency on WS-A is weaker than assumed here, since WS-A adds no control to the terminal stack header and its UI change lands in the project table's TERMINAL header instead — WS-B may proceed in parallel and reconcile only against WS-A's step S5; (2) an unrelated defect surfaced en route — `open_remote_subprocess` in `src-tauri/src/system.rs` is a plain synchronous `pub fn` that escapes the never-block-the-UI rule today only because it never waits on its child, and it must become `async fn` + `spawn_blocking` in the same step that makes it read the child's output.
- **2026-07-27** — WS-D research written (`docs/research/terminal-vietnamese-ime-root-cause-3.md`); `-2.md` carries its `Status: superseded by` line. The chain now has a mechanism traced line-to-symptom rather than hypothesised, and it is in the guard rather than in xterm: `useWkImeGuard.js`'s `classify()` excludes any multi-character `key` whose payload is all ASCII letters, on the stated assumption that an OpenKey carrier "always contains a non-ASCII Vietnamese char" — true of the *correction* path the guard was designed against, false of the *auto-restore* path, whose carrier is the original raw ASCII. Verified independently at `src/composables/useWkImeGuard.js:196`. **Still a candidate, not a confirmed cause**: it holds only if the restore carrier arrives untagged by keyCode 229, and only the Mac dump can settle that. Note also that round 2's claim "Chromium never tags 229, which is why VS Code is fine" is now refuted as a blanket statement — xterm.js #5887 reproduces in Chrome per its own issue — so no VS Code technique exists to copy, and any fix must be ours.
- **2026-07-27** — WS-E planned (`hygiene-jul27.md`). Two of its three items changed shape under investigation and the backlog's own item descriptions above are now the stale version: neither the WebSocket message nor the icon 404 is emitted by app code at all — both are WebKit's own network-layer logs, which JavaScript cannot suppress. Item 1 is therefore expected reconnect churn rather than a defect. Item 2 remains fixable, but by a different route than "stop logging it": `src/utils/projectIcon.js`'s host branch requests an icon URL for every project, ignoring `projectStore.projectIcons` — a map that is already complete and null-aware on the host as well as the companion (verified: `App.vue` fills it via `refreshProjectIcons`, and `projectStore.js` documents every project id as present with an explicit `null` when there is no icon). Consulting it first means never requesting a URL already known to 404. **Open design point for implementation**: the map can lag a freshly added icon, so an id *absent* from the map must fall through to the request rather than be treated as icon-less — only an explicit `null` may suppress it.
- **2026-07-28** — WS-F implemented. `CountBadgeWrap.vue` gained `deleteArmed`/`deleteTitle` props rendering a `fa-solid fa-trash` icon on `.cell-badge-bottom` (already unused by these two buttons), styled with the same `#ef4444` the count badge already uses locally — no new colour, no new z-index. `ProjectTable.vue` wires `p.delete_on_push`/`p.delete_on_pull` (flat fields on the project record, confirmed in `src/composables/useProjectConfig.js` and `src-tauri/src/projects.rs`) through `!isStop(p, 'push'|'pull')` so the marker disappears during that direction's STOP state, per the recommendation above. Titles state the consequence, not the flag name. **Unverified**: whether the new bottom-corner icon visually collides with anything at the narrow breakpoint (`ProjectTable.vue`'s `.dry-group` squeeze) — this needs a screen, not a diff, and was left as reported rather than guessed at.
- **2026-07-28** — WS-D evidence kit and gated patch prepared (addendum appended to `terminal-vietnamese-ime-root-cause-3.md`, doc body above it left untouched per the research-doc immutability rule). `scripts/capture-ime-evidence.sh` is the runnable capture procedure (Mac-only, prints the numbered recipe including the `Main.html`-vs-`localhost` inspector trap, stages the console confirm+clear snippet via `pbcopy`, then starts `npm run tauri dev`). The `classify()` replacement is written in full in the addendum, gated on Candidate 1 being confirmed by the Mac dump — **not applied**, `src/composables/useWkImeGuard.js` is unmodified. Design: swaps the old "is the payload all ASCII letters" test for "is `ev.key` a named DOM key value" (`NAMED_KEYS` set), which is what the exclusion was always meant to test; per-shape claimant table confirms exactly one shape's claimant changes (the ASCII restore carrier, `null` → `multiCarrier` — the fix) plus one incidental latent-bug fix (function keys, previously mis-routed into `multiCarrier` by the old regex). Explicitly does not address Candidate 2 (229-tagged carrier with no claimable `beforeinput`) or Candidate 3 (decomposed-Unicode backspace-count mismatch) — either of those being confirmed instead means this patch must not be applied.
- **2026-07-30** — Plan-consolidation pass (`plan-consolidator`). Active plans 17 → 13: two closed as shipped (`terminal-ime-input-layer-separation.md` → `7ce5804`, `terminal-mobile-keyboard-viewport.md` → `873e849`, both verified against source rather than against their own status lines, and both previously absent from `docs/index.md`), one audit rehomed to `docs/research/audit-release-window-jul28.md` per `docs.C2`, one roadmap rehomed to `docs/arch/remote-views-roadmap.md` per `docs.A2`. Three candidate merges evaluated and all three declined — shared symptom is not shared definition of correct, which is the same test that made this session withdraw a #4+#9 merge earlier. `docs/index.md` updated for every move; five source-comment references to the moved paths remain and are D-10. Five new drift findings recorded (D-7..D-11), one of them the first `wrong`-severity entry in this table. Four questions escalated to the owner, listed in the consolidation section above. **This tracker's own name is now inaccurate**: `backlog-jul27.md` carries a date suffix from a batch that is finished and it has since absorbed the jul30 T-1..T-10 batch, so it should be renamed to `backlog.md` — the project's single perpetual plan index, no date, since it does not belong to one batch any more (`docs.A2` permits the suffix as a disambiguator but the name must still describe the content). The rename was **not** executed: it has a large inbound-reference fan-out (`docs/index.md` plus most active plan docs, several of which other agents are editing right now), so it belongs in a quiet pass, not a concurrent one.
- **2026-07-31** — Doc-drift + status pass (`leftovers`, read-only against code, only this tracker edited). WS-E item 3 (hard-wrap sweep) settled as **finished**: commits `1c00e3b`/`3c5188e` (2026-07-28) land it inside `1.20.0..HEAD`, and `docs/ref/dragging-titlebar.md` still reads one-line-per-paragraph today. WS-D, T-1, T-2, T-10 marked closed against `docs/plan/terminal-input-jul31.md` §1 (owner-verified on real hardware, 2026-07-31). T-3 marked closed against `de801c2`, which moved `ag-usage-pin-vs-live.md` to `done/`. WS-A and WS-B re-checked and left `planned` — their plan docs are still active, not moved to `done/`. `README.md` and `IntroModal.vue` also checked in the same pass (CLAUDE.md's "Feature changed?" rule): one stale line found and fixed (README's Under-the-Hood section still said Smart Stack Launcher ran commands "inside the native macOS terminal", contradicted by the shipped in-app-terminal DEV/BUILD launch); everything else in both files already matched current behaviour, including the terminal/Vietnamese-IME and Pre-allow AGY Commands sections.
